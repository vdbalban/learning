#include <stdio.h>
#include <stdlib.h>
// calcul avec des variables

int main()
{
    int result = 0;
    double resultat = 0;
    result = 5 - 3;
    resultat = 5 / 3.5;
    printf("le resultat est %d\n", result); // %d pour le int , variable
    printf("le resultat est %lf\n", resultat); // %lf pour le double, variable


    return 0;
}